# Fig 5e - Threshold Comparison

Horizontal bar chart of predicted counts with a side panel showing the delta of structure ratio between models.

Files
- `plot.py` generates the figure.
- `fig5_e_threshold_comparison.png` is the output image.

Run
```bash
python plot.py
```
